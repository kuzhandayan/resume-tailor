Read the job description at jd/$1.md (or, if I pasted the JD directly in
chat, save it there first). Then tailor a resume per CLAUDE.md:
copy base_stable_template.tex to out/$1.tex, edit only the content zones,
verify every cap in Rule 3, and report in the required format.
Do not print the .tex to chat.
